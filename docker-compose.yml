version: '3.8'

services:
  app:
    container_name: myhomekitchen-app
    build: .
    ports:
      - "8083:8083"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
